#include <iostream>
#include <stack>
#include <map>
#include <set>
#include <vector>
#include <string>

using namespace std;

typedef set<int> Set;
map<Set, int> SetStack;
vector<Set> Setcache;

Set find_Set(int n) {
    if(map.count())
}


int main() {
    stack<int> s;
    string op;
    cin >> n;
    while(n--) {
        cin >> op;
        if(op[0] == 'P') s.push(ID(Set()));
        else if(op[0] == 'D') s.push(s.top());
        else {
            Set x1 = Setcache[s.top()]; s.pop();
            Set x2 = Setcache[s.top()]; s.pop();
            if(op[0] == 'U') s.push()




        }
    }
    
    

}